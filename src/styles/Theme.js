
import { createTheme } from '@mui/material/styles';

const myTheme = createTheme({
  palette: {
    mode: 'dark',
    background: {
        default: "#303030"
      }
  },
});

export default myTheme;